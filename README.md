# Namma Velai Website MVP

Responsive static website for Namma Velai.

## Before publishing
Open `app.js` and replace:
- `whatsappNumber: "919999999999"` with the real Namma Velai WhatsApp number.
- `channelUrl: "#"` with the real WhatsApp Channel URL.
- Every job `url:"#"` with the verified official notification/application URL.

The listed jobs are sample/demo content for the website structure. Verify each notification and official URL before publishing.

## Deploy
Upload the files to GitHub and import the repository into Vercel. No build command is needed; this is a static site.

## Next development phase
Admin dashboard, database/Google Sheets, job detail pages, job expiry, SEO district pages, analytics, application-assistance order tracking, sponsored listings and affiliate sections.
